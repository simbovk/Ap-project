import 'package:flutter/material.dart';

class FinishBuying extends StatelessWidget {
  const FinishBuying({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp();
  }
}
